<x-layouts.app title="Fixed Asset & Depresiasi"><section class="mx-auto max-w-7xl px-4 py-8 lg:px-8"><h1 class="text-2xl font-bold tracking-tight">Fixed Asset & Depresiasi</h1><p class="mt-2 text-slate-500">Register aset, straight-line depreciation, fiscal control, dan balanced posting.</p>@if(session('status'))<div class="mt-4 rounded-xl bg-emerald-50 p-4">{{ session('status') }}</div>@endif @if($errors->any())<div class="mt-4 rounded-xl bg-red-50 p-4 text-red-700">{{ $errors->first() }}</div>@endif
<div class="mt-6 grid gap-5 lg:grid-cols-3"><form method="post" action="/admin/fixed-assets/categories" class="grid gap-3 rounded-2xl border bg-white p-5">@csrf<h2 class="font-bold">Kategori Aset</h2><input name="code" required placeholder="Kode kategori" class="rounded-xl border p-3"><input name="name" required placeholder="Nama kategori" class="rounded-xl border p-3"><input type="number" min="1" name="default_useful_life_months" required placeholder="Umur default (bulan)" class="rounded-xl border p-3"><button class="rounded-xl bg-slate-900 p-3 text-white">Simpan kategori</button></form>
<form method="post" action="/admin/fixed-assets" class="grid gap-3 rounded-2xl border bg-white p-5">@csrf<h2 class="font-bold">Register Aset</h2><select name="fixed_asset_category_id" required class="rounded-xl border p-3">@foreach($categories as $c)<option value="{{ $c->id }}">{{ $c->code }} — {{ $c->name }}</option>@endforeach</select><select name="project_id" class="rounded-xl border p-3"><option value="">Tanpa proyek</option>@foreach($projects as $p)<option value="{{ $p->id }}">{{ $p->code }}</option>@endforeach</select><input name="code" required placeholder="Kode aset" class="rounded-xl border p-3"><input name="name" required placeholder="Nama aset" class="rounded-xl border p-3"><input type="date" name="acquisition_date" required class="rounded-xl border p-3"><input type="date" name="depreciation_start_date" required class="rounded-xl border p-3"><input type="number" step=".01" name="acquisition_cost" required placeholder="Harga perolehan" class="rounded-xl border p-3"><input type="number" step=".01" min="0" name="residual_value" value="0" required placeholder="Nilai residu" class="rounded-xl border p-3"><input type="number" min="1" name="useful_life_months" required placeholder="Umur manfaat bulan" class="rounded-xl border p-3"><button class="rounded-xl bg-sky-700 p-3 text-white">Register aset</button></form>
<form method="post" action="/admin/finance/mappings" class="grid gap-3 rounded-2xl border bg-white p-5">@csrf<h2 class="font-bold">Mapping Depresiasi</h2><input type="hidden" name="event_type" value="asset_depreciation"><select name="entry_side" class="rounded-xl border p-3"><option value="expense_debit">Depreciation Expense Debit</option><option value="accumulated_credit">Accumulated Depreciation Credit</option></select><select name="account_id" required class="rounded-xl border p-3">@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->code }} — {{ $a->name }}</option>@endforeach</select><button class="rounded-xl bg-violet-700 p-3 text-white">Simpan mapping</button></form></div>
<div class="mt-8 space-y-3">@forelse($assets as $asset)<article class="rounded-2xl border bg-white p-5"><div class="flex flex-wrap items-center justify-between gap-3"><div><strong>{{ $asset->code }} — {{ $asset->name }}</strong><p class="text-sm text-slate-500">{{ $asset->category?->name }} · Cost {{ number_format((float)$asset->acquisition_cost,2,',','.') }} · {{ $asset->useful_life_months }} bulan</p></div><form method="post" action="/admin/fixed-assets/{{ $asset->id }}/depreciate" class="flex flex-wrap gap-2">@csrf<select name="fiscal_period_id" required class="rounded-xl border p-2">@foreach($periods as $period)<option value="{{ $period->id }}">{{ $period->name }}</option>@endforeach</select><input type="date" name="depreciation_date" value="{{ today()->toDateString() }}" required class="rounded-xl border p-2"><input type="hidden" name="idempotency_key" value="depreciation-{{ $asset->id }}-{{ today()->format('Ym') }}"><button class="rounded-xl bg-slate-800 px-4 text-white">Post depresiasi</button></form></div></article>@empty<div class="rounded-2xl border border-dashed p-8 text-center">Belum ada aset tetap.</div>@endforelse</div>
<div class="mt-8 overflow-x-auto rounded-2xl border bg-white"><table class="w-full text-sm"><thead><tr><th>Tanggal</th><th>Aset</th><th>Nilai</th><th>Posted</th></tr></thead><tbody>@forelse($depreciations as $d)<tr><td>{{ $d->depreciation_date->format('d/m/Y') }}</td><td>{{ $d->asset?->code }}</td><td>{{ number_format((float)$d->amount,2,',','.') }}</td><td>{{ $d->posted_at->format('d/m/Y H:i') }}</td></tr>@empty<tr><td colspan="4" class="p-8 text-center">Belum ada depresiasi.</td></tr>@endforelse</tbody></table></div></section></x-layouts.app>
