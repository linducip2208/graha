<x-layouts.app title="Periode Fiskal"><div class="page-container"><a href="/admin/finance" class="text-sm font-bold text-[var(--brand-primary)]">← Finance Workspace</a><x-ui.page-header title="Periode Fiskal" subtitle="Kontrol tanggal posting dan status closing accounting." />@if(session('status'))<div class="mt-4 rounded-xl bg-emerald-50 p-4">{{ session('status') }}</div>@endif @if($errors->any())<div class="mt-4 rounded-xl bg-red-50 p-4 text-red-700">{{ $errors->first() }}</div>@endif
@if(auth()->user()->hasPermission('finance.manage',app(\App\Support\Tenancy\CurrentCompany::class)->id()))<div class="mb-4 flex justify-end no-print"><button type="button" class="btn-brand inline-flex min-h-[42px] items-center gap-2 rounded-xl px-4 text-sm font-bold shadow-sm" data-drawer-open="period-create-drawer"><x-ui.icon name="plus" class="h-4 w-4" />Periode Baru</button></div>
<x-ui.drawer id="period-create-drawer" title="Periode Fiskal Baru">
<form method="post" action="/admin/finance/periods" class="grid gap-4">@csrf<input name="name" placeholder="Nama periode" required class="rounded-xl border p-3"><input type="date" name="starts_at" required class="rounded-xl border p-3"><input type="date" name="ends_at" required class="rounded-xl border p-3"><button class="rounded-xl bg-slate-900 p-3 font-bold text-white">Buka periode</button></form>
</x-ui.drawer>
@endif
<div class="mt-6 overflow-x-auto rounded-2xl border bg-white"><table class="w-full text-sm"><thead><tr><th>Periode</th><th>Mulai</th><th>Selesai</th><th>Status</th><th>Ditutup</th></tr></thead><tbody>@forelse($periods as $period)<tr><td class="font-bold">{{ $period->name }}</td><td>{{ $period->starts_at->format('d/m/Y') }}</td><td>{{ $period->ends_at->format('d/m/Y') }}</td><td>{{ strtoupper($period->status) }}</td><td>{{ $period->closed_at?->format('d/m/Y H:i')??'—' }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center">Belum ada periode.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $periods->links() }}</div></div></x-layouts.app>
