<x-layouts.app title="Accounting Mapping"><section class="mx-auto max-w-5xl px-4 py-8 sm:px-6"><h1 class="text-3xl font-black">Accounting Mapping</h1><p class="mt-2 text-slate-500">Akun ditentukan per perusahaan dan event; tidak ada nomor akun hard-coded.</p>@if(session('status'))<div class="mt-4 rounded-xl bg-emerald-50 p-4">{{ session('status') }}</div>@endif @if($errors->any())<div class="mt-4 rounded-xl bg-red-50 p-4 text-red-700">{{ $errors->first() }}</div>@endif<form method="post" action="/admin/finance/mappings" class="mt-8 grid gap-3 rounded-2xl border bg-white p-5 md:grid-cols-4">@csrf<select name="event_type" class="rounded-xl border p-3"><option value="goods_receipt">Goods Receipt</option><option value="vendor_invoice">Vendor Invoice</option><option value="material_issue_project">Material Issue Project</option><option value="production_completion">Production Completion</option></select><select name="entry_side" class="rounded-xl border p-3"><option value="debit">Debit</option><option value="credit">Credit</option></select><select name="account_id" required class="rounded-xl border p-3"><option value="">Pilih akun</option>@foreach($accounts as $account)<option value="{{ $account->id }}">{{ $account->code }} · {{ $account->name }}</option>@endforeach</select><button class="rounded-xl bg-violet-700 p-3 text-white">Simpan mapping</button></form><div class="mt-8 overflow-x-auto rounded-2xl border bg-white"><table class="w-full text-sm"><thead><tr><th>Event</th><th>Sisi</th><th>Akun</th><th>Efektif</th></tr></thead><tbody>@forelse($mappings as $mapping)<tr><td>{{ $mapping->event_type }}</td><td>{{ strtoupper($mapping->entry_side) }}</td><td>{{ $mapping->account?->code }} · {{ $mapping->account?->name }}</td><td>{{ $mapping->effective_from??'Selalu' }}</td></tr>@empty<tr><td colspan="4" class="p-8 text-center">Belum ada mapping.</td></tr>@endforelse</tbody></table></div></section></x-layouts.app>
