<x-layouts.app title="Accounting Mapping"><div class="page-container"><x-ui.page-header title="Accounting Mapping" subtitle="Akun ditentukan per perusahaan dan event; tidak ada nomor akun hard-coded." />@if(session('status'))<div class="mt-4 rounded-xl bg-emerald-50 p-4">{{ session('status') }}</div>@endif @if($errors->any())<div class="mt-4 rounded-xl bg-red-50 p-4 text-red-700">{{ $errors->first() }}</div>@endif<div class="mb-4 flex justify-end no-print"><button type="button" class="btn-brand inline-flex min-h-[42px] items-center gap-2 rounded-xl px-4 text-sm font-bold shadow-sm" data-drawer-open="mapping-create-drawer"><x-ui.icon name="plus" class="h-4 w-4" />Tambah Mapping</button></div>
<x-ui.drawer id="mapping-create-drawer" title="Accounting Mapping">
<form method="post" action="/admin/finance/mappings" class="grid gap-4">@csrf<select name="event_type" class="rounded-xl border p-3"><option value="goods_receipt">Goods Receipt</option><option value="vendor_invoice">Vendor Invoice</option><option value="material_issue_project">Material Issue Project</option><option value="production_completion">Production Completion</option></select><select name="entry_side" class="rounded-xl border p-3"><option value="debit">Debit</option><option value="credit">Credit</option></select><select name="account_id" required class="rounded-xl border p-3"><option value="">Pilih akun</option>@foreach($accounts as $account)<option value="{{ $account->id }}">{{ $account->code }} · {{ $account->name }}</option>@endforeach</select><button class="rounded-xl bg-violet-700 p-3 text-white">Simpan mapping</button></form>
</x-ui.drawer>
<div class="mt-8 overflow-x-auto rounded-2xl border bg-white"><table class="w-full text-sm"><thead><tr><th>Event</th><th>Sisi</th><th>Akun</th><th>Efektif</th></tr></thead><tbody>@forelse($mappings as $mapping)<tr><td>{{ $mapping->event_type }}</td><td>{{ strtoupper($mapping->entry_side) }}</td><td>{{ $mapping->account?->code }} · {{ $mapping->account?->name }}</td><td>{{ $mapping->effective_from??'Selalu' }}</td></tr>@empty<tr><td colspan="4" class="p-8 text-center">Belum ada mapping.</td></tr>@endforelse</tbody></table></div></div></x-layouts.app>
