<x-layouts.app title="Chart of Accounts"><section class="mx-auto max-w-6xl px-4 py-8 lg:px-8"><a href="/admin/finance" class="text-sm font-bold text-[var(--brand-primary)]">← Finance Workspace</a><h1 class="mt-3 text-2xl font-bold tracking-tight">Chart of Accounts</h1><p class="mt-2 text-slate-500">Struktur akun perusahaan untuk seluruh posting ERP.</p>@if(session('status'))<div class="mt-4 rounded-xl bg-emerald-50 p-4">{{ session('status') }}</div>@endif @if($errors->any())<div class="mt-4 rounded-xl bg-red-50 p-4 text-red-700">{{ $errors->first() }}</div>@endif
@if(auth()->user()->hasPermission('finance.manage',app(\App\Support\Tenancy\CurrentCompany::class)->id()))<div class="mb-4 flex justify-end no-print"><button type="button" class="btn-brand inline-flex min-h-[42px] items-center gap-2 rounded-xl px-4 text-sm font-bold shadow-sm" data-drawer-open="finance-account-drawer</button></div>
<x-ui.drawer id="{finance-account-drawer}" title="{Akun Baru}">
<form method="post" action="/admin/finance/accounts" class="grid gap-4">@csrf<input name="code" placeholder="Kode akun" required class="rounded-xl border p-3"><input name="name" placeholder="Nama akun" required class="rounded-xl border p-3 md:col-span-2"><select name="type" class="rounded-xl border p-3">@foreach(['asset','liability','equity','revenue','expense'] as $v)<option>{{ $v }}</option>@endforeach</select><select name="normal_balance" class="rounded-xl border p-3"><option value="debit">Debit</option><option value="credit">Kredit</option></select><button class="rounded-xl bg-slate-900 p-3 font-bold text-white md:col-start-5">Tambah akun</button></form>
</x-ui.drawer>
@endif
<div class="mt-6 overflow-x-auto rounded-2xl border bg-white"><table class="w-full min-w-[650px] text-sm"><thead><tr><th>Kode</th><th>Nama Akun</th><th>Tipe</th><th>Saldo Normal</th><th>Status</th></tr></thead><tbody>@forelse($accounts as $account)<tr><td class="font-mono font-bold">{{ $account->code }}</td><td>{{ $account->name }}</td><td>{{ strtoupper($account->type) }}</td><td>{{ strtoupper($account->normal_balance) }}</td><td>{{ $account->is_active?'AKTIF':'NONAKTIF' }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center">Belum ada akun.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $accounts->links() }}</div></section></x-layouts.app>
