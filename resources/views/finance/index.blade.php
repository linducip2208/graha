<x-layouts.app title="Finance & Accounting"><section class="mx-auto max-w-7xl px-6 py-10"><h1 class="text-3xl font-black">Finance & Accounting</h1><p class="mt-2 text-slate-500">Double-entry, period controlled, dan tanpa hard-coded nomor akun.</p>@if(session('status'))<div class="mt-4 rounded-xl bg-emerald-50 p-4">{{ session('status') }}</div>@endif @if($errors->any())<div class="mt-4 rounded-xl bg-red-50 p-4 text-red-700">{{ $errors->first() }}</div>@endif<div class="mt-8 grid gap-6 xl:grid-cols-3"><form method="post" action="/admin/finance/accounts" class="grid gap-3 rounded-2xl border bg-white p-5">@csrf<h2 class="font-bold">Chart of Accounts</h2><input name="code" placeholder="Kode akun" required class="rounded-xl border p-3"><input name="name" placeholder="Nama akun" required class="rounded-xl border p-3"><select name="type" class="rounded-xl border p-3">@foreach(['asset','liability','equity','revenue','expense'] as $v)<option>{{ $v }}</option>@endforeach</select><select name="normal_balance" class="rounded-xl border p-3"><option value="debit">Debit</option><option value="credit">Kredit</option></select><button class="rounded-xl bg-slate-800 p-3 text-white">Tambah akun</button></form><form method="post" action="/admin/finance/periods" class="grid gap-3 rounded-2xl border bg-white p-5">@csrf<h2 class="font-bold">Periode Fiskal</h2><input name="name" placeholder="Nama periode" required class="rounded-xl border p-3"><input type="date" name="starts_at" required class="rounded-xl border p-3"><input type="date" name="ends_at" required class="rounded-xl border p-3"><button class="rounded-xl bg-slate-800 p-3 text-white">Buka periode</button></form><form method="post" action="/admin/finance/journals" class="grid gap-3 rounded-2xl border bg-white p-5">@csrf<h2 class="font-bold">Jurnal Manual</h2><input type="date" name="journal_date" value="{{ now()->toDateString() }}" required class="rounded-xl border p-3"><input name="reference" placeholder="Referensi unik" required class="rounded-xl border p-3"><input name="description" placeholder="Deskripsi" required class="rounded-xl border p-3"><select name="debit_account_id" required class="rounded-xl border p-3"><option value="">Akun debit</option>@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->code }} {{ $a->name }}</option>@endforeach</select><select name="credit_account_id" required class="rounded-xl border p-3"><option value="">Akun kredit</option>@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->code }} {{ $a->name }}</option>@endforeach</select><input type="number" step=".01" min=".01" name="amount" placeholder="Nominal" required class="rounded-xl border p-3"><button class="rounded-xl bg-sky-700 p-3 text-white">Post jurnal</button></form></div><div class="mt-8 overflow-x-auto rounded-2xl border bg-white"><table class="w-full text-sm"><thead><tr><th>Nomor</th><th>Tanggal</th><th>Deskripsi</th><th>Debit</th><th>Kredit</th><th>Status</th></tr></thead><tbody>@forelse($journals as $j)<tr><td>{{ $j->number }}</td><td>{{ $j->journal_date->format('d/m/Y') }}</td><td>{{ $j->description }}</td><td>{{ $j->entries->reduce(fn($c,$e)=>bcadd($c,$e->debit,2),'0') }}</td><td>{{ $j->entries->reduce(fn($c,$e)=>bcadd($c,$e->credit,2),'0') }}</td><td>{{ strtoupper($j->status) }}</td></tr>@empty<tr><td colspan="6" class="p-8 text-center">Belum ada jurnal.</td></tr>@endforelse</tbody></table></div>{{ $journals->links() }}</section></x-layouts.app>
