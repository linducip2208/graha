<x-layouts.app title="General Ledger"><div class="page-container"><a href="/admin/finance" class="text-sm font-bold text-[var(--brand-primary)]">← Finance Workspace</a><x-ui.page-header title="General Ledger" /><p class="mt-2 text-slate-500">Jurnal posted bersifat immutable dan selalu seimbang.</p>@if(session('status'))<div class="mt-4 rounded-xl bg-emerald-50 p-4">{{ session('status') }}</div>@endif @if($errors->any())<div class="mt-4 rounded-xl bg-red-50 p-4 text-red-700">{{ $errors->first() }}</div>@endif
@if(auth()->user()->hasPermission('accounting.post',app(\App\Support\Tenancy\CurrentCompany::class)->id()))<details class="mt-6 rounded-2xl border bg-white p-5"><summary class="cursor-pointer font-black">Buat Jurnal Manual</summary><form method="post" action="/admin/finance/journals" class="mt-5 grid gap-3 md:grid-cols-3">@csrf<input type="date" name="journal_date" value="{{ now()->toDateString() }}" required class="rounded-xl border p-3"><input name="reference" placeholder="Referensi unik" required class="rounded-xl border p-3"><input name="description" placeholder="Deskripsi" required class="rounded-xl border p-3"><select name="debit_account_id" required class="rounded-xl border p-3"><option value="">Akun debit</option>@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->code }} — {{ $a->name }}</option>@endforeach</select><select name="credit_account_id" required class="rounded-xl border p-3"><option value="">Akun kredit</option>@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->code }} — {{ $a->name }}</option>@endforeach</select><input type="number" step=".01" min=".01" name="amount" placeholder="Nominal" required class="rounded-xl border p-3"><button class="rounded-xl bg-[var(--brand-primary)] p-3 font-bold text-white md:col-start-3">Post jurnal</button></form></details>@endif
<div class="mt-6 overflow-x-auto rounded-2xl border bg-white"><table class="w-full min-w-[760px] text-sm"><thead><tr><th>Nomor</th><th>Tanggal</th><th>Deskripsi</th><th>Debit</th><th>Kredit</th><th>Status</th></tr></thead><tbody>@forelse($journals as $j)<tr><td class="font-mono font-bold">{{ $j->number }}</td><td>{{ $j->journal_date->format('d/m/Y') }}</td><td>{{ $j->description }}</td><td>{{ number_format((float)$j->entries->sum('debit'),2,',','.') }}</td><td>{{ number_format((float)$j->entries->sum('credit'),2,',','.') }}</td><td>{{ strtoupper($j->status) }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center">Belum ada jurnal.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $journals->links() }}</div></div></x-layouts.app>
