<x-layouts.app title="Project Costing & EAC"><section class="mx-auto max-w-7xl px-4 py-8 lg:px-8"><h1 class="text-2xl font-bold tracking-tight">Project Costing & EAC</h1><p class="mt-2 text-slate-500">Actual dari ledger jurnal, committed dari PO aktif, dan forecast tersimpan sebagai snapshot.</p>@if(session('status'))<div class="mt-4 rounded-xl bg-emerald-50 p-4">{{ session('status') }}</div>@endif @if($errors->any())<div class="mt-4 rounded-xl bg-red-50 p-4 text-red-700">{{ $errors->first() }}</div>@endif
@if(auth()->user()->hasPermission('finance.manage',app(\App\Support\Tenancy\CurrentCompany::class)->id()))<form method="post" action="/admin/project-costing/forecasts" class="mt-6 grid gap-3 rounded-2xl border bg-white p-5 lg:grid-cols-5">@csrf<select name="project_id" required class="rounded-xl border p-3">@foreach($projects as $project)<option value="{{ $project->id }}">{{ $project->code }} — {{ $project->name }}</option>@endforeach</select><input type="date" name="forecast_date" value="{{ today()->toDateString() }}" required class="rounded-xl border p-3"><input type="number" step=".01" min="0" name="cost_to_complete" required placeholder="Cost to complete" class="rounded-xl border p-3"><input name="basis" placeholder="Dasar estimasi/revisi" class="rounded-xl border p-3"><input type="hidden" name="idempotency_key" value="forecast-{{ Str::uuid() }}"><button class="rounded-xl bg-sky-700 p-3 text-white">Simpan forecast</button></form>@endif
<div class="mt-8 overflow-x-auto rounded-2xl border bg-white"><table class="w-full min-w-[1050px] text-sm"><thead><tr><th>Proyek</th><th>Budget/RAP</th><th>Actual</th><th>Committed PO</th><th>Cost to Complete</th><th>EAC</th><th>Variance</th><th>Contract</th></tr></thead><tbody>@forelse($projects as $project)@php($s=$summaries[$project->id])<tr><td><strong>{{ $project->code }}</strong><br>{{ $project->name }}</td>@foreach(['budget','actual','committed','cost_to_complete','eac','variance','contract_value'] as $key)<td class="font-mono">{{ number_format((float)$s[$key],2,',','.') }}</td>@endforeach</tr>@empty<tr><td colspan="8" class="p-8 text-center">Belum ada proyek.</td></tr>@endforelse</tbody></table></div></section></x-layouts.app>
