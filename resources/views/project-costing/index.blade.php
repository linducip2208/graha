<x-layouts.app title="Project Costing & EAC"><div class="page-container"><x-ui.page-header title="Project Costing & EAC" /><p class="mt-2 text-slate-500">Actual dari ledger jurnal, committed dari PO aktif, dan forecast tersimpan sebagai snapshot.</p>@if(session('status'))<div class="mt-4 rounded-xl bg-emerald-50 p-4">{{ session('status') }}</div>@endif @if($errors->any())<div class="mt-4 rounded-xl bg-red-50 p-4 text-red-700">{{ $errors->first() }}</div>@endif
@if(auth()->user()->hasPermission('finance.manage',app(\App\Support\Tenancy\CurrentCompany::class)->id()))<div class="mb-4 flex justify-end no-print"><button type="button" class="btn-brand inline-flex min-h-[42px] items-center gap-2 rounded-xl px-4 text-sm font-bold shadow-sm" data-drawer-open="forecast-create-drawer"><x-ui.icon name="plus" class="h-4 w-4" />Buat Forecast</button></div>

@endif
<div class="mt-8 overflow-x-auto rounded-2xl border bg-white"><table class="w-full min-w-[1050px] text-sm"><thead><tr><th>Proyek</th><th>Budget/RAP</th><th>Actual</th><th>Committed PO</th><th>Cost to Complete</th><th>EAC</th><th>Variance</th><th>Contract</th></tr></thead><tbody>@forelse($projects as $project)@php($s=$summaries[$project->id])<tr><td><strong>{{ $project->code }}</strong><br>{{ $project->name }}</td>@foreach(['budget','actual','committed','cost_to_complete','eac','variance','contract_value'] as $key)<td class="font-mono">{{ number_format((float)$s[$key],2,',','.') }}</td>@endforeach</tr>@empty<tr><td colspan="8" class="p-8 text-center">Belum ada proyek.</td></tr>@endforelse</tbody></table></div><x-ui.drawer id="forecast-create-drawer" title="Forecast EAC">
<form method="post" action="/admin/project-costing/forecasts" class="grid gap-4">@csrf<select name="project_id" required class="rounded-xl border p-3">@foreach($projects as $project)<option value="{{ $project->id }}">{{ $project->code }} — {{ $project->name }}</option>@endforeach</select><input type="date" name="forecast_date" value="{{ today()->toDateString() }}" required class="rounded-xl border p-3"><input type="number" step=".01" min="0" name="cost_to_complete" required placeholder="Cost to complete" class="rounded-xl border p-3"><input name="basis" placeholder="Dasar estimasi/revisi" class="rounded-xl border p-3"><input type="hidden" name="idempotency_key" value="forecast-{{ Str::uuid() }}"><button class="rounded-xl bg-[var(--brand-primary)] p-3 text-white">Simpan forecast</button></form>
</x-ui.drawer>
</div></x-layouts.app>
