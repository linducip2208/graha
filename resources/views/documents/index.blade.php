<x-layouts.app title="Document Control"><section class="mx-auto max-w-7xl px-6 py-10"><h1 class="text-3xl font-black">Document Control</h1><p class="mt-2 text-slate-500">File privat, versioned, dan terikat hash SHA-256.</p>@if(session('status'))<div class="mt-5 rounded-xl bg-emerald-50 p-4">{{ session('status') }}</div>@endif @if($errors->any())<div class="mt-5 rounded-xl bg-red-50 p-4 text-red-700">{{ $errors->first() }}</div>@endif
<form method="post" action="/admin/documents" enctype="multipart/form-data" class="mt-8 grid gap-4 rounded-2xl border bg-white p-6 md:grid-cols-2">@csrf<label>Jenis dokumen<input name="document_type" required class="mt-2 w-full rounded-xl border p-3" placeholder="contract, policy, drawing"></label><label>Judul<input name="title" required class="mt-2 w-full rounded-xl border p-3"></label><label>File PDF/JPG/PNG<input type="file" name="file" required accept=".pdf,.jpg,.jpeg,.png" class="mt-2 w-full rounded-xl border p-3"></label><label>Alasan/tujuan versi<input name="change_reason" required class="mt-2 w-full rounded-xl border p-3"></label><button class="rounded-xl bg-sky-700 px-5 py-3 font-bold text-white md:col-span-2">Daftarkan dokumen</button></form>
<div class="mt-8 overflow-x-auto rounded-2xl border bg-white"><table class="w-full text-sm"><thead><tr><th>Nomor</th><th>Judul</th><th>Jenis</th><th>Status</th><th>Versi</th><th></th></tr></thead><tbody>@forelse($documents as $document)<tr><td class="font-mono">{{ $document->number }}</td><td>{{ $document->title }}</td><td>{{ $document->document_type }}</td><td>{{ $document->workflow_status }}</td><td>{{ $document->versions->first()?->version??0 }}</td><td>@if($document->versions->first())<a class="font-bold text-sky-700" href="/admin/document-versions/{{ $document->versions->first()->id }}/download">Unduh</a>@endif</td></tr>@empty<tr><td colspan="6" class="p-8 text-center">Belum ada dokumen.</td></tr>@endforelse</tbody></table></div>{{ $documents->links() }}</section></x-layouts.app>
