<x-layouts.app title="Organisasi"><section class="mx-auto max-w-7xl px-6 py-10"><p class="font-semibold text-sky-700">{{ $company->code }}</p><h1 class="text-2xl font-bold tracking-tight">Organisasi</h1>@if(session('status'))<div class="mt-5 rounded-xl bg-emerald-50 p-4">{{ session('status') }}</div>@endif @if($errors->any())<div class="mt-5 rounded-xl bg-red-50 p-4 text-red-700">{{ $errors->first() }}</div>@endif
<div class="mt-8 grid gap-8 lg:grid-cols-2"><article class="rounded-2xl border bg-white p-6"><h2 class="text-xl font-bold">Cabang</h2><form method="post" action="/admin/branches" class="mt-5 grid gap-3 sm:grid-cols-[120px_1fr_auto]">@csrf<input name="code" placeholder="Kode" required class="rounded-xl border p-3"><input name="name" placeholder="Nama cabang" required class="rounded-xl border p-3"><button class="rounded-xl bg-sky-700 px-5 py-3 font-bold text-white">Tambah</button></form><div class="mt-6 overflow-x-auto"><table class="w-full text-sm"><thead><tr><th>Kode</th><th>Nama</th><th>Status</th></tr></thead><tbody>@forelse($branches as $branch)<tr><td>{{ $branch->code }}</td><td>{{ $branch->name }}</td><td>{{ $branch->is_active?'Aktif':'Nonaktif' }}</td></tr>@empty<tr><td colspan="3">Belum ada cabang.</td></tr>@endforelse</tbody></table></div>{{ $branches->links() }}</article>
<article class="rounded-2xl border bg-white p-6"><h2 class="text-xl font-bold">Departemen</h2><form method="post" action="/admin/departments" class="mt-5 grid gap-3">@csrf<input name="code" placeholder="Kode" required class="rounded-xl border p-3"><input name="name" placeholder="Nama departemen" required class="rounded-xl border p-3"><select name="branch_id" class="rounded-xl border p-3"><option value="">Lintas cabang</option>@foreach($branches as $branch)<option value="{{ $branch->id }}">{{ $branch->name }}</option>@endforeach</select><button class="rounded-xl bg-sky-700 px-5 py-3 font-bold text-white">Tambah departemen</button></form><div class="mt-6 overflow-x-auto"><table class="w-full text-sm"><thead><tr><th>Kode</th><th>Nama</th><th>Cabang</th></tr></thead><tbody>@forelse($departments as $department)<tr><td>{{ $department->code }}</td><td>{{ $department->name }}</td><td>{{ $department->branch?->name??'Semua' }}</td></tr>@empty<tr><td colspan="3">Belum ada departemen.</td></tr>@endforelse</tbody></table></div>{{ $departments->links() }}</article></div></section></x-layouts.app>
